# Winter Video Agent 协作约定

## 项目边界

这是一个全新的 Winter Video Agent 仓库。它负责理解需求、选择制作流程、组织项目状态、调用工具、执行质检和交付。

- 不依赖旧的 `Winter-video-workspace` 才能运行。
- 除非用户明确要求迁移历史能力，否则不要从旧工作区复制约定、目录或实现。
- Agent 仓库不是素材仓库、视频项目仓库、渲染目录或依赖缓存。
- 每期视频的素材、临时文件、预览、渲染结果和交付文件都应位于仓库外的独立项目目录。

## 能力层级

在本仓库制作或续作视频时，先读取 `skills/winter-video-agent/SKILL.md`。当前口播预览已接通数据驱动配方；其余流程仍按文档规划，不能当成已实现能力。

当前只维护三条一级视频流程：

1. `talking-head`：真人口播或已有配音的视频剪辑。
2. `product-promo`：产品宣传、功能演示和商业镜头。
3. `knowledge-explainer`：从主题、文章或文稿生成知识讲解视频。

每个视频项目必须选择一条主流程。其他流程的组件可以作为素材使用，但不能让一个项目同时由多条主流程控制。

`threeui` 是组件资产来源，不是第四条视频流程。

## 上游仓库

仓库根目录下的四个上游目录均为固定版本的 Git 子模块：

- `video-talkcraft/`
- `video-shotcraft/`
- `anything2explainer/`
- `threeui/`

默认规则：

- 将子模块视为只读源码快照，不在其中创建项目、安装依赖、渲染视频或保存临时文件。
- 不直接修改上游代码。需要复用时，将所需文件复制到本项目未来的自有目录，再做适配。
- 复制代码时记录来源仓库、提交和原始路径，便于以后比较上游更新。
- 更新子模块必须是一次明确的同步工作，更新后重新执行兼容性检查。
- 用户已确认获得 Vincent 对 TalkCraft、ShotCraft 和 Anything2Explainer 代码复用及二次开发的授权。仓库中的授权记录用于说明来源，不替代双方的原始授权凭证。

## 自有代码

未来实现按职责放置：

- `core/`：项目生命周期、素材登记、运行目录、状态和公共命令。
- `workflows/`：三条独立生产流程。
- `library/`：已纳入本项目维护的卡片、动画、转场、音效和组件。
- `skills/`：Codex 使用入口和渐进式说明。
- `scripts/`：可重复、可验证的确定性操作。

不要为了表面统一，把三条流程改造成同一种内部时间轴或同一套镜头模型。统一对外操作和交付状态，保留各流程成熟的内部结构。

## 运行纪律

- 不在仓库根目录或任何上游子模块中生成单期视频文件。
- 不把 `node_modules`、下载素材、模型缓存、渲染缓存、预览和成片提交到 Git。
- 单期修改默认沿用原项目、原主流程和已确认的视觉方向，只重做受影响的阶段或镜头。
- 对某一期视频的反馈先记录为该项目的决定；只有用户明确要求，才升级为 Agent 的全局规则。
- 没有必要时不新增网页、桌面界面或重型时间轴编辑器。Codex 内可调用的工作流优先。

## 验证与提交

- 文档变更至少执行 `git diff --check`。
- 子模块变更要检查 `git submodule status`，并确认子模块工作区干净。
- 代码变更执行与改动直接相关的最小有效验证；不要用重复实现的测试代替真实产物检查。
- 提交前检查没有素材、缓存、密钥、个人隐私文件或误生成的大文件。
