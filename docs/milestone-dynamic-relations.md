# 动态关系讲解阶段

2026-09-26。目标：先用一段实际口播复现参考片的复杂关系展开，沉淀可复用能力，再交用户审阅。

## 本次交付

智能助手项目的原片40.5–56.5秒，独立镜头 `server-house`，产物 `preview-v6-relations.mp4`，16秒、1280×960、30fps、480帧、有原音频。

参考样片194–208秒数据库段的中心锚点、分类累积、灰线到亮线方法，转换为本期的服务器/房子、OpenCode/管家、权限、操作分支。透明剖面房子由内置imagegen生成；沿用既有AI机器人和真实终端截图。终端只展示界面，不声称已完成示意里的整理文件操作。

## 已纳入自有配方

- image.keyframes：位置、尺寸的确定性插值，先大后小让出关系图空间。
- connector：绑定对象边缘的曲线，跟随关键帧移动，在 active_at 开始点亮。
- protected_regions.start/end：按分镜生效的保护区，适应景别与人物位置变化。
- 保守运动路径、连线引用和存活区间的校验。
- 不依赖素材的 relations-example.json，说明这些机制可以用于其他内容。

沿用已有项目、配方、计划版本、素材登记与单镜头预览命令。没有修改上游子模块，没有新建UI，没有生成新的生产流程。实现为自有代码；参考片只提供视觉方法。

## 验证及限制

首次V5渲染后的抽帧发现人物前倾到右侧，原保护区不足，解释图压脸。只针对明确故障修正一次：V6改用左侧布局并增加本段保护区。V5保留为故障记录，不作为交付推荐。V6抽帧确认左侧图与脸分离，媒体检查确认16秒、480帧和音频流；7项相关计划测试通过。

无覆层遮罩，保留原字幕与原声。本次真实跑通 --shot，输出未重做前段。用户尚未评价新样片；视觉效果和阅读节奏待确认。时序仍依据原字幕人工抽帧，未接通自动字级对齐。图片使用静帧素材，真实终端也为截图；尚未接入视频素材层、分镜缓存重组或完整视频制作。保护区人工声明，非人脸追踪；连线框体校验保守。
