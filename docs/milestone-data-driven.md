# 数据驱动口播配方阶段记录

2026-09-23。

## 已完成

- 先将原型阶段提交为 2e245ea 并推送 origin/main。
- 将专用模板中的内容、素材 ID、坐标、画幅和时序抽成外部项目 semantic-plan.json。
- 建立 progressive-explanation 配方，支持文本、图片、面板、关系线及三类入场运动。
- 增加分镜校验、按哈希归档、预览来源快照和用户反馈记录。
- 渲染前检测输入变更、文本溢出和静态保护区冲突；项目写入互斥，失败产物留在 work，成功后才进入 preview。
- 增加仓库配套 Skill 入口，AGENTS.md 指引新会话按项目记录续作。

## 验证

- 智能助手原片：数据驱动渲染 9.6 秒 V3，1280×960，音视频均存在。未重新生成 AI 图片。
- 域名原片：同一模板、不同文字布局、1280×720，生成静帧验证数据复用。代表帧发现旧布局会接近脸部后，只改外部计划加入保护区并右移，未改渲染代码。此项目未做口播时序对齐，也不是正式成片。
- 三项定向测试通过：无效时序/缺失素材；保护区碰撞；版本改变后反馈失效隔离。
- Skill 结构校验与 git diff --check 通过。

## 能力边界

Codex 仍负责理解口播和创作分镜，脚本不自动生成语义计划。当前是短片段预览闭环，未实现完整转写对齐、全片镜头调度、自动局部缓存合成、资产自动检索与清理。保护区靠项目声明，未做人脸自动跟踪。

使用外部已安装 Playwright/Chrome，运行时路径由参数传入，不复制到仓库。本次没有安装全局 Skill；在该仓库通过 AGENTS.md 和 skills/winter-video-agent/SKILL.md 使用。

## 下一里程碑

在一个完整口播项目内规划多个语义镜头，先确认一个代表镜头，再支持按镜头修改和渲染。对接 TalkCraft 时间戳与镜头渲染前，先保留现有配方的输入、输出和用户决定契约，避免另起一套不可续接的工作流。
