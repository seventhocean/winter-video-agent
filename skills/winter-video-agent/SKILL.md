---
name: winter-video-agent
description: 在 winter-video-agent 项目中制作或续作真人口播增强视频，使用外部项目档案、语义分镜和递进解释配方生成可审阅样片，并按用户反馈修改。
---

# Winter Video Agent

这是仓库配套入口，命令从仓库根目录执行。能力状态：真人口播的短片段、图片/文字/关系线动效预览可运行；完整转写对齐、产品宣传和知识讲解生产线尚未接通，不能承诺已支持。

## 开始或续作

- 先读仓库 AGENTS.md。单期项目、依赖、素材和渲染结果在仓库外；上游子模块只读。
- 已有项目先运行 `python3 -B -m core.cli inspect PROJECT`，读取当前计划、用户决定和产物。不要只依靠聊天历史重建计划。
- 新项目用 `core.cli doctor` 检查基础工具；用 `core.cli new PROJECT --source SOURCE --start SECONDS --duration SECONDS` 登记选段。当前默认输出宽 1280，等比画幅；更换画幅应同步 project.json 和分镜 canvas。
- 检查原片画面、原字幕和口播内容。没有精确转写时标明语义时序依据，不把手工抽帧定位说成字级对齐。

## 制作决策

- 一个语义段先明确要让观众理解什么，再选择主体、证据、关系和结论。使用 [递进解释配方](../../library/recipes/progressive-explanation/RECIPE.md) 处理累积信息或关系展开。
- 生成式物体和场景优先使用可用的 AI 生图工具；真实界面和操作证据使用用户素材或真实采集。文字、数字和关系线保持可编辑。
- 人物、字幕和关键动作需要留出空间。分镜中的 protected_regions 是用户项目数据，不是全局固定布局。
- 一次先做代表片段，渲染后供用户确认。已有明确授权可以继续；本项目当前用户要求先看结果，不自动多轮审美重渲。

## 执行

具体命令、分镜字段和环境参数见 [执行说明](references/execution.md)。

将计划保存为外部项目 input/semantic-plan.json，登记素材后先校验，再 set 保存版本。只改文字、素材、坐标或时序时修改计划，不改模板代码。

每次产物关联计划哈希、素材哈希和模板快照。用户明确给出评价后才运行 review 记录决定，不将生成成功记为审美通过。

修改一段时优先用计划的 shots 划分和渲染命令的 `--shot ID` 做独立预览；目前不具备全片自动局部缓存重组，不宣称已经有。修复明确故障后只验证相关部分。

用户要求无覆层遮罩时，在本期计划设置 `no_mask: true`。校验拒绝 panel 和文字背景填充，文字用轮廓阴影保持可读；不要偷偷添加半透明黑色底板。此选项不替代对图片素材本身的目视检查。
